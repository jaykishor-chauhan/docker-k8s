
apiVersion: v1
kind: Service
metadata:
  name: clusterip-svc
  labels:
    env: demo
spec:
  type: ClusterIP # this is the default type so we can also remove it, it will work
  ports:
  - port: 80  
    targetPort: 80
  selector:
    env: demo